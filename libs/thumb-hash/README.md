# thumb-hash

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build thumb-hash` to build the library.

## Running unit tests

Run `nx test thumb-hash` to execute the unit tests via [Jest](https://jestjs.io).
